#include <stdio.h>
int main(){
    
    int valor;
    puts("Digite um valor inteiro");
    scanf("%d", &valor);
    printf("O triplo do seu valor é %d\nO seu quadrado é %d\nE sua metade é %.1f", valor*3, valor*valor, (float)valor/2);

    return 0;
}
